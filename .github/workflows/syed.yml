name: python

on:
  push:
  schedule:
    - cron: '35 1 3 * *'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - uses: actions/checkout@v4

      - name: python code
        run: python sm/hello1.py
